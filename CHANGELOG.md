# 更新日誌 Changelog

本專案的功能更新紀錄，最新在最上面。日期為實際上線（push）日。

## 2026-09-07

- **例句可以點來聽了**：每個單字的兩句例句各多一顆 🔈，列表模式與卡片模式都有
  - 例句發音用 Edge TTS（en-US-JennyNeural、語速 -25%）預錄全站 680 句，放在 `audio/ex/`；
    單字發音維持原本的音色不變
  - 唸出來的是乾淨的句子，畫面上的「1.」「2.」編號不會被唸進去
  - 萬一某句還沒有音檔，會自動退回瀏覽器內建語音，不會變成沒聲音
- 幕後：新增 `tools/gen_example_tts.py`，之後上新的一週跑一次 `--all` 就會補齊缺的例句音檔

## 2026-09-01

- **新增家長設定**（點右上角金幣、輸入密碼進入）
  - 直接設定金幣數量，或一鍵 +1000
  - 一鍵解鎖所有商店物品（不扣金幣）
  - **備份／還原**：把存檔複製成一段文字存到備忘錄，之後可以貼回來還原
  - 加這個是因為金幣與已買的物品只存在該台裝置的瀏覽器裡，換瀏覽器、
    清除瀏覽資料、或 iPhone 長期沒開都可能整份不見
- **2026 Fall 開學，W1–W8 上線**：共 80 個字，中文釋義 100%、真人發音 79/80
  （`giggle` 撞到當日語音配額，隔天補上）
  - 打開網站就會停在「今天該背的兩個字」，不用自己找週次
  - 這學期的 W10／W11 是複習＋期中考、W20／W21 是複習＋期末考，那四週沒有新單字
- **「今天」改成中午換日**：老師下午才上 ESL 課教新字，所以早上打開看到的是昨天教的
  那兩個字（正好複習），過了中午才換成今天新教的。以前照午夜換日，早上出門前會看到
  還沒教的字
- 幕後：OCR 流程改成可以直接吃手機翻拍的 HEIC 照片（這學期的單字本是拍的不是掃的），
  且一週跨兩頁時會兩頁一起辨識，才抓得到週次與完整的 10 個字
- 幕後：新增資料檔自動接線與完整性檢查，避免漏掉中文釋義或發音

## 2026-08-31

老師改成「每天背兩個單字」，這一版把網站從查閱工具改成會記進度的練習工具。

- **新增「⭐ 今天」每日模式**：依日期自動算出今天該背哪兩個字（照該週單字表順序切，Day1 = 第 1-2 個字），開啟網站預設就停在今天的進度，不用自己找週次
  - 首頁多一張今日進度卡：今天是第幾週第幾天、兩個字各自完成了沒、連續完成幾天
  - 週末與國定假日不加新字，改成複習本週到目前為止學過的字
- **新增「🔁 複習錯過的字」**：答錯過而且還沒練熟的字會自動收進這裡，錯最多的排最前面；同一個字答對 3 次就自動畢業
- **新增熟練度追蹤**：每個字的對錯次數會記下來，單字列表上用 ✅ 熟了 / 🔁 要複習 / 📖 練習中 標示
- **Quiz 強化**
  - 新增反向題型：除了看定義選單字，也會看單字選定義
  - 「今天」模式下誘答選項改從整週抽，不會變成二選一
  - 優先出今天還沒答對的字
- **Spell 大幅強化**
  - 出題時給提示（英文定義＋中文釋義）並先唸一次，不再是盲猜字形
  - 依單字長度挖 1–3 個空格（原本固定只挖一個字母），要全部拼完才算答對
  - 新增「🔈 再聽一次」
- **金幣改成每個字每天只給一次**：避免同一個字重複刷；完成當天兩個字另外加發 50 金幣
- **可離線使用**：加到主畫面後沒網路也能開（新增 service worker）

修正與整理：

- 修正右上角金幣列的 HTML 破版（金幣圖示與數字跑到樣式外面）
- 修正圖片快取設定：以前每次重新整理都會重抓約 5MB 圖片，現在正常快取
- 修正 W21 `ran` 的詞性標成名詞（應為動詞）
- 統一詞性寫法（`(pronoun)`→`(pron.)`、`(prep)`→`(prep.)`、斜線前後不留空格）
- 移除 13 張換裝系統改版後沒人用的圖片（約 2.8MB）與對應死 CSS
- `batch_tts.py` 撞到 Gemini 每日配額時會乾淨中止整批，不再每個字空燒 3.5 分鐘；已產生的音檔照樣保留並推上去

## 2026-06-17

- **W12 補上發音與中文釋義** → 期末考範圍（W12–W21，共 9 週 90 字）發音＋中文全部到位
- **W17 / W19 / W20 / W21 補上中文釋義**

## 2026-06-14

- **新增「中文釋義」開關**：單字列表上方可一鍵展開／收合該週所有單字的中文意思，預設收合、切換週次自動歸零
- **W13–W15 補上 Achernar 真人發音**
- **Quiz 強化**
  - 答題後可按「🔈 聽答案發音」聽正解（答對、答錯都能聽）
  - 答錯時自動把正確選項標成綠色，看著＋聽著一起訂正
  - 答對／答錯的回饋語音（Correct / Try again）改用 Achernar 真人語音
- **下拉選單新增「📘 複習 (W13-W16)」範圍**

## 2026-06-13

- **Avatar Shop 換裝系統上線**：用練習賺到的金幣幫角色換裝（整套服裝、帽子、鞋子、配件、寵物、背景場景）
- **可加到 iPhone 主畫面像 App 使用**（PWA）：專屬 App 圖示（公主城堡場景），開啟為全螢幕、沒有瀏覽器網址列

## 2026-06-08

- **單字發音改用預錄 Achernar 真人音檔**（無音檔時自動退回瀏覽器語音），發音更自然

## 2026-05-03

- 下拉選單新增「期中考範圍 (W1-W9)」「期末考範圍 (W12-W21)」快選

## 2026-04-26

- 新增 Reading／Science 類別切換
- 擴充新學期單字的 OCR 流程可自動略過非單字頁

---

> 更完整的逐筆紀錄見 [GitHub commit 歷史](https://github.com/KENJISHIH/ESL-vocab-practice/commits/main)。
